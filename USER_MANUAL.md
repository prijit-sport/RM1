# คู่มือการใช้งานเบื้องต้น — ระบบจัดการอพาร์ทเมนต์ RM1

คู่มือนี้อธิบายวิธีใช้งานระบบสำหรับผู้ใช้งานทั่วไป (พนักงาน/ผู้จัดการ/ผู้ดูแลระบบ) ไม่เจาะลึกเรื่องเทคนิค

---

## 1. การเข้าสู่ระบบ

1. เปิดเว็บเบราว์เซอร์ไปที่ URL ของระบบ
2. กรอกอีเมลและรหัสผ่าน
3. กด "เข้าสู่ระบบ"

**หมายเหตุ**: ระบบจะบล็อกการ login ชั่วคราวถ้าใส่รหัสผ่านผิดติดต่อกันหลายครั้ง (ป้องกันการเดารหัสผ่าน) รอสักครู่แล้วลองใหม่

---

## 2. ระดับสิทธิ์ผู้ใช้งาน

| ระดับ | ทำอะไรได้บ้าง |

|---|---|
| **ผู้ดูแลระบบ (Admin)** | เข้าถึงทุกส่วนของระบบ จัดการผู้ใช้งาน กำหนดสิทธิ์ |
| **ผู้จัดการ (Manager)** | จัดการห้องพัก การจอง สัญญา ใบแจ้งหนี้ ซ่อมบำรุง ดูรายงาน |
| **พนักงาน (Staff)** | ทำงานประจำวัน เช่น จดมิเตอร์ บันทึกการซ่อมบำรุง จัดการการจอง |
| **ผู้ใช้ทั่วไป (User)** | เข้าถึงข้อมูลพื้นฐาน สิทธิ์จำกัด |

ถ้าเมนูไหนไม่แสดงหรือกดแล้วขึ้น "Forbidden" แปลว่าบัญชีของคุณไม่มีสิทธิ์เข้าถึงส่วนนั้น ให้ติดต่อผู้ดูแลระบบ

---

## 3. การจัดการห้องพัก

- เมนู **"ห้องพัก"** — ดูรายการห้องทั้งหมด สถานะ (ว่าง/มีผู้เช่า/ซ่อมบำรุง)
- กด **"เพิ่มห้องพัก"** เพื่อเพิ่มห้องใหม่ ระบุเลขห้อง ประเภท ราคา ชั้น โซน
- แก้ไข/ลบห้องได้จากปุ่มในตารางรายการ

---

## 4. การจัดการผู้เช่าและการจอง

1. เพิ่มข้อมูลผู้เช่าที่เมนู **"ผู้เช่า"** ก่อน (ชื่อ อีเมล เบอร์โทร เลขบัตรประชาชน)
2. ไปที่เมนู **"เข้าพัก"** (Booking) → กด **"เพิ่มการจอง"**
3. เลือกห้อง เลือกผู้เช่า ระบุวันเข้าพัก/ออก
4. **ระบบรองรับผู้เช่าร่วมสูงสุด 3 คนต่อการจอง** (เลือกผู้เช่าคนที่ 2 และ 3 เพิ่มเติมได้ในฟอร์มเดียวกัน)
5. เมื่อผู้เช่าเข้าพักจริง กด **"ยืนยัน (Confirm)"** ห้องจะเปลี่ยนสถานะเป็น "มีผู้เช่า" อัตโนมัติ

---

## 5. การจัดการสัญญาเช่า

- เมนู **"สัญญาเช่า"** — สร้างสัญญาใหม่ ระบุค่าเช่ารายเดือน วันเริ่ม-สิ้นสุดสัญญา เงื่อนไขต่างๆ
- **ข้อควรทราบ**: ระบบไม่ออกใบแจ้งหนี้ค่าเช่าอัตโนมัติจากสัญญา พนักงานต้องสร้างใบแจ้งหนี้ค่าเช่าด้วยตัวเองทุกเดือนผ่านเมนู "ใบแจ้งหนี้"
- หน้ารายงาน/แดชบอร์ดจะแสดง "สัญญาใกล้หมดอายุ" ให้ติดตามได้

---

## 6. การออกใบแจ้งหนี้

### ใบแจ้งหนี้ค่าเช่า (สร้างด้วยมือ)

1. ไปที่เมนู **"ใบแจ้งหนี้"** → กด **"เพิ่มใบแจ้งหนี้"**
2. เลือกการจอง (Booking) ที่เกี่ยวข้อง กรอกจำนวนเงิน ภาษี วันครบกำหนด

### ใบแจ้งหนี้ค่าน้ำ-ไฟ (กึ่งอัตโนมัติจากมิเตอร์)

1. ไปที่เมนู **"มิเตอร์น้ำ/ไฟ"** เลือกห้อง → บันทึกค่ามิเตอร์ประจำเดือน
2. ระบบคำนวณค่าใช้จ่ายอัตโนมัติจากส่วนต่างมิเตอร์ × อัตราค่าบริการ แล้วสร้างใบแจ้งหนี้ร่างให้
3. ตรวจสอบตัวเลขในหน้ายืนยัน แล้วกด **"บันทึก"** เพื่อยืนยันใบแจ้งหนี้

### การรับชำระเงิน

- เปิดใบแจ้งหนี้ที่ต้องการ กด **"ทำเครื่องหมายว่าชำระแล้ว (Mark as Paid)"** ระบุวิธีชำระเงิน

---

## 7. การแจ้งซ่อมบำรุง

1. เมนู **"ซ่อมบำรุง"** → กด **"แจ้งซ่อม"**
2. เลือกห้อง ระบุประเภทปัญหา วันที่แจ้ง (ทั้งสองช่องนี้**จำเป็นต้องกรอก**)
3. เมื่อเริ่มดำเนินการซ่อม กด **"เริ่มดำเนินการ"** และเมื่อเสร็จ กด **"เสร็จสิ้น"**

---

## 8. รายงานและแดชบอร์ด

หน้า **"แดชบอร์ด"** และ **"รายงาน"** แสดงสรุปภาพรวม:

- จำนวนห้องว่าง/มีผู้เช่า
- รายได้รวม (จากการจอง) และยอดใบแจ้งหนี้ค้างชำระ
- สัญญาที่ใกล้หมดอายุหรือหมดอายุแล้ว
- งานซ่อมบำรุงที่รอดำเนินการ

---

## 9. ปัญหาที่พบบ่อย

| อาการ | สาเหตุที่เป็นไปได้ | วิธีแก้ |

|---|---|---|
| Login ไม่ได้ ขึ้น "ลองใหม่ภายหลัง" | ใส่รหัสผ่านผิดหลายครั้งติดกัน ระบบบล็อกชั่วคราว | รอ 1 นาทีแล้วลองใหม่ |
| กดเมนูแล้วขึ้น "Forbidden" (403) | บัญชีไม่มีสิทธิ์เข้าถึงส่วนนั้น | ติดต่อผู้ดูแลระบบขอปรับสิทธิ์ |
| แจ้งซ่อมไม่ได้ ขึ้น error | ไม่ได้กรอก "ประเภทปัญหา" หรือ "วันที่แจ้ง" | กรอกทั้งสองช่องให้ครบ (บังคับ) |
| ค้นหา/กรองข้อมูลไม่เจอที่ต้องการ | ข้อมูลอาจถูก cache ไว้ชั่วคราว | รอสักครู่แล้วลองใหม่ หรือรีเฟรชหน้า |
